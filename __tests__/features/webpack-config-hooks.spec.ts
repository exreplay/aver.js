import { testFeature } from '../utils/feature';

testFeature('webpack-config-hooks', () => {
  /**
   * Actual test is running inside aver-config file
   */
  // eslint-disable-next-line @typescript-eslint/no-empty-function
  test('should run hooks correctly', () => {});
});
